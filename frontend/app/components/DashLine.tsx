import React from 'react';
import { View } from 'react-native';

const DashLine = () => {
	return (
		<View
			style={{
				borderStyle: 'dashed',
				borderWidth: 0.5,
				borderColor: '#A0A0A0',
			}}
		/>
	);
};

export default DashLine;
