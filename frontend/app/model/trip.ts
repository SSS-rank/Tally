export interface TripListItemProps {
	title: string;
	nationName: string;
	date: string;
	image: string;
}

export interface TripInfo {
	title: string;
	location: number;
	type: string;
	startDay: string;
	endDay: string;
}
